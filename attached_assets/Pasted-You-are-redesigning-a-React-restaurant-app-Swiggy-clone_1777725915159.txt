You are redesigning a React restaurant app (Swiggy clone) to look like a cinematic
dark food delivery UI — bold hero, floating food image, red+green accents, premium feel.
The stack is: React functional components + hooks + Redux (cart) + React Router + plain CSS.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 1 — index.css (do this first, everything depends on it)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Add at the top of index.css:

@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@700;800;900&family=Nunito:wght@400;500;600;700&family=Space+Grotesk:wght@500;600;700&display=swap');

Add these CSS variables in :root:

:root {
  --bg: #0A0A0A;
  --surface: #151515;
  --surface-2: #1E1E1E;
  --red: #E8231A;
  --red-glow: rgba(232,35,26,0.35);
  --green: #22C55E;
  --green-glow: rgba(34,197,94,0.30);
  --yellow: #F5A623;
  --text: #FFFFFF;
  --text-muted: #9CA3AF;
  --border: rgba(255,255,255,0.08);
  --border-card: rgba(255,255,255,0.10);
  --r-sm: 8px; --r-md: 16px; --r-lg: 24px; --r-pill: 999px;
  --f-display: 'Poppins', sans-serif;
  --f-body: 'Nunito', sans-serif;
  --f-mono: 'Space Grotesk', sans-serif;
}
body { background: var(--bg); color: var(--text); font-family: var(--f-body); }

Add these keyframes:
@keyframes fadeUp { from{opacity:0;transform:translateY(28px)} to{opacity:1;transform:translateY(0)} }
@keyframes floatY { 0%,100%{transform:translateY(0) rotate(0deg)} 50%{transform:translateY(-14px) rotate(8deg)} }
@keyframes shimmer { 0%{background-position:200% 0} 100%{background-position:-200% 0} }
@keyframes pulseRed { 0%,100%{box-shadow:0 8px 24px var(--red-glow)} 50%{box-shadow:0 8px 36px rgba(232,35,26,0.65)} }
@keyframes bounceIn { 0%{transform:scale(0.5);opacity:0} 70%{transform:scale(1.2)} 100%{transform:scale(1);opacity:1} }

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 2 — Header.js
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Add useState(false) for scrolled, useEffect with window scroll listener
- position: fixed, transparent by default
- On scroll > 40px: add class that sets background rgba(10,10,10,0.85) + backdrop-filter blur(12px) + border-bottom 1px solid var(--border)
- Logo: font-family var(--f-display), font-weight 900, color var(--yellow)
- Nav links: var(--f-body) weight 600, color var(--text-muted), hover → var(--text)
- Cart icon: relative positioned, red badge (useSelector from Redux), badge uses bounceIn animation when count changes
- Sign Up button: background var(--green), color #000, font-family var(--f-display), border-radius var(--r-pill), hover: box-shadow 0 0 20px var(--green-glow)
- NO border or box-shadow on nav by default

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 3 — Body.js (Hero + Featured + Grid)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
HERO:
- min-height 100vh, CSS grid 1fr 1fr, align-items center
- background: radial-gradient(ellipse at 80% 50%, #2a0a0a 0%, #0A0A0A 60%)
- padding-top 80px (navbar offset)

Left column:
- Small uppercase label (Nunito 600, letter-spacing 2.5px, var(--text-muted)) — animation fadeUp 0.5s 0.1s both
- H1 Poppins 900, clamp(2.6rem,5vw,4.8rem), line-height 1.1 — animation fadeUp 0.5s 0.25s both
- Last word/line of H1 "anytime!" → color var(--red), font-style italic, use <span>
- Subtext Nunito 400, var(--text-muted), max-width 380px — animation fadeUp 0.5s 0.4s both
- Two CTAs row — animation fadeUp 0.5s 0.55s both:
  • "Order Now" — background var(--red), Poppins 700, border-radius pill, animation pulseRed 2.5s infinite, hover scale(1.05)
  • "See Menu" — no bg, border-bottom 2px solid var(--yellow), hover color var(--yellow)

Right column (position relative):
- Large circular food image: 460x460px, border-radius 50%, object-fit cover — animation fadeUp 0.7s 0.2s both
- 5 absolutely positioned decorators: 2x cubes (28px, var(--green) bg, border-radius 6px), 1x ring (38px, border 6px solid var(--green)), 2x dots (12px circles, one red one green) — all use animation floatY with different durations (3s-5s) and animation-delay values (0s, 0.5s, 0.8s, 1s, 1.5s)
- "Our Happy Customers" pill: absolute top-right, background rgba(255,255,255,0.06), backdrop-filter blur(12px), border 1px solid rgba(255,255,255,0.12), border-radius var(--r-lg) — 3 stacked avatar circles + green "+2k" badge + label text

FEATURED ROW:
- Section title: Poppins 800, var(--text), with ::after red underline (44px wide, 3px, var(--red))
- Flex row, gap 20px, overflow-x auto, scrollbar hidden
- Each card: var(--surface) bg, border var(--border-card), border-radius var(--r-md), padding 16px, padding-top 52px, position relative
- Circular image pops out: position absolute, top -35px, left 50%, translateX(-50%), 80x80px, border-radius 50%, border 3px solid var(--surface)
- Price: Space Grotesk 700, var(--green)
- Stars: var(--yellow)
- Cart icon button: var(--green) bg, hover box-shadow var(--green-glow)
- Card hover: border-color rgba(232,35,26,0.45), translateY(-6px), transition 200ms

RESTAURANT GRID:
- grid-template-columns repeat(auto-fill, minmax(280px,1fr)), gap 24px
- Cards: same dark style, see RestaurantCard rules below

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 4 — RestaurantCard.js
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- background var(--surface), border 1px solid var(--border-card), border-radius var(--r-md), overflow hidden
- Hover: border-color rgba(232,35,26,0.4), translateY(-6px), transition all 200ms ease-out
- Image wrapper: overflow hidden, aspect-ratio 16/9
- Image hover: scale(1.05), transition 200ms
- Rating chip: absolute top-right of image, background var(--yellow), color #000, Space Grotesk 700, 0.75rem, padding 3px 10px, border-radius var(--r-sm)
- Card body padding 16px
- Name: Nunito 700, var(--text)
- Meta/cuisine: 0.85rem, var(--text-muted)
- Delivery time: var(--red), Nunito 600

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 5 — Shimmer.js
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Render 8 shimmer cards in same grid as restaurant grid
- Each card: var(--surface) bg, border var(--border-card), border-radius var(--r-md), overflow hidden
- Image area: height 180px, no border-radius
- Title bar: height 18px, margin 16px, border-radius 6px
- Two lines: height 13px, margin 0 16px 8px
- All shimmer elements: background linear-gradient(90deg,#151515,#282828,#151515), background-size 200% 100%, animation shimmer 1.5s infinite linear
- NO gray shimmer

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 6 — Cart.js
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Page background var(--bg), padding 100px 60px
- Title: Poppins 800, 2rem, var(--text)
- Each item row: flex, gap 16px, border-bottom 1px solid var(--border)
- Item name: Nunito 700, var(--text)
- Item price: Space Grotesk 700, var(--green)
- Qty +/- buttons: border 1px solid var(--red), color var(--red), hover fill red
- Total: Nunito 700, price in Space Grotesk, var(--green), larger
- Checkout button: width 100%, background var(--red), Poppins 700, border-radius var(--r-md), padding 16px, animation pulseRed 2.5s infinite, hover scale(1.02)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 7 — RestaurantMenu.js
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Background var(--bg), padding 100px 60px
- Page title: Poppins 800, var(--text)
- Category title: Nunito 700, 1.1rem, border-bottom 1px solid var(--border)
- Each item: flex row, justify-content space-between, border-bottom 1px solid var(--border)
- Left: name (Nunito 700), description (0.85rem, var(--text-muted)), price (Space Grotesk 700, var(--green))
- Right: 90x90px thumbnail (border-radius var(--r-sm)) + Add button below it
- Add button: border 1.5px solid var(--red), color var(--red), transparent bg, hover/added → bg var(--red) color white, click → scale(0.93)
- Veg dot: green circle 10px (var(--green)), Non-veg: brownish dot

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
RULES — never break these
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✗ Never hardcode hex values in JSX inline styles — always use CSS variables
✗ Never use white or light backgrounds anywhere
✗ Never use Inter, Roboto, or system fonts — always var(--f-display/body/mono)
✗ Never use blue or purple accents
✗ Never use gray shimmer — dark shimmer only (#151515 → #282828)
✗ Never center hero text — left-aligned only
✗ Never use box-shadow as default card style — only on hover or for glow buttons
✗ All animations must use the keyframes defined in index.css — no inline animation styles